# Deployment requirements (used by Render, see render.yaml).
# Same as requirements.txt but with opencv-python-headless instead of
# opencv-python -- a headless Linux server doesn't have (and doesn't
# need) the GUI libraries (Qt) the regular package pulls in; server.py
# never calls a GUI function (cv2.imshow/selectROI). Keep using
# requirements.txt locally if you still use main.py's CLI manual-ROI
# calibration flow, which does need the GUI build.
opencv-python-headless>=4.8
numpy>=1.24
matplotlib>=3.7
pandas>=2.0
scipy>=1.10
flask>=3.0
Pillow>=10.0
xlrd>=2.0
gunicorn>=21.0
